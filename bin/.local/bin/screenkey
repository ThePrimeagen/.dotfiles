#!/usr/bin/env bash

while [ : ]; do
    pkill screenkey
    /usr/bin/screenkey -s medium --key-mode composed --scr 1 -p bottom --geometry 1210x300+2000+1100 --timeout 1.5 &
    sleep 15
    echo "Restarting"
done
